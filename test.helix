io.print("Reading ./test.helix")

let fd = file("test.helix")
fd.open("r")

let text = fd.read()
let words = text.split(" ")

io.print("Words:", words.length)

for word in words {
    io.print(word, "is", word.length, "characters long")
}
